import {render} from 'test';
import AirportDetailContainer from './AirportDetailContainer';

describe('AirportDetailContainer', () => {
  it('renders', async () => {
    throw new Error('You need to write a test for this component!');
  });
});
