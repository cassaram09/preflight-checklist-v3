import {render} from 'test';
import NavlogContainer from './NavlogContainer';

describe('NavlogContainer', () => {
  it('renders', async () => {
    throw new Error('You need to write a test for this component!');
  });
});
