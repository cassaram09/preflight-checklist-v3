import {render} from 'test';
import EmergencyProceduresContainer from './EmergencyProceduresContainer';

describe('EmergencyProceduresContainer', () => {
  it('renders', async () => {
    throw new Error('You need to write a test for this component!');
  });
});
